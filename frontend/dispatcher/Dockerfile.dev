# Development Dockerfile for the dispatcher Next.js app.
# Source code is bind-mounted at runtime (see docker-compose.dev.yml),
# so this image only installs dependencies — no COPY . . here.

FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

CMD ["npm", "run", "dev"]
