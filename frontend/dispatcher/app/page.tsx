// Dispatcher console root page — placeholder until the auth flow is built.
export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <h1 className="text-2xl font-bold">FreightProof SA — Dispatcher</h1>
    </main>
  );
}
