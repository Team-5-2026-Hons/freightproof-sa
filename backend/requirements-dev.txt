-r requirements.txt
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0
respx>=0.21.0,<1.0.0  # httpx transport mock — PP client unit tests
ruff==0.15.22  # pinned: 0.16.0 expanded default rule set (UP045/I001/B008/etc.), breaking CI
mypy>=1.13.0,<2.0.0
pypdf>=6.0,<7.0  # reads rendered audit-pack PDFs back in tests
pip-audit>=2.10.0,<3.0.0
