# Spikes

This directory holds exploratory spike code. Each spike lives in its own subdirectory named after the ticket it investigates (e.g. `fp-001-hedera/`).

Spike code is throwaway — it exists to answer a specific technical question and is never imported by the application. Once the spike produces a decision, the findings go into the relevant ticket and the production implementation is written from scratch in the correct application layer.
